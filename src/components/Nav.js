import React from "react";

function Nav() {
    return <div class="header">
        <h1>Employee Finder</h1>
        <p>Use the search box to narrow the employee directory. You can also click on carrots to filter by name.</p>
    </div>
}

export default Nav;